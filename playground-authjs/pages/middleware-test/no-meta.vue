<!--
  Page with no `auth` meta defined.

  The global middleware protects this page by default because it runs on
  every route. Unauthenticated users are redirected to the sign-in page
  and authenticated users are granted access.
-->
<template>
  <h1>no-meta</h1>
</template>
